{
    "version": "1.0",
    "configurations": [
        {
            "name": "Run consensus test",
            "type": "python",
            "request": "launch",
            "stopOnEntry": true,
            "program": "${workspaceRoot}/pyngs/sam/test_consensus.py",
            "cwd": "${workspaceRoot}",
            "console": "integratedTerminal"
        }
    ]
}
